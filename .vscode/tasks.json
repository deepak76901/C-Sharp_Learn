{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build and run",
      "type": "shell",
      "command": "mcs ${fileBasename}; if ($?) {mono ${fileBasenameNoExtension}.exe}",

      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": []
    }
  ]
}
